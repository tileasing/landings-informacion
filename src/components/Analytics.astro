---
// Microsoft Clarity — proyecto "landings informacion" (guia.oautos.com.mx).
// El tag SOLO se inyecta en el build de producción (`astro build`), no en
// `astro dev`, para no ensuciar los datos con tráfico local.
// El Project ID es público (viaja en el cliente).
const enabled = import.meta.env.PROD;
---

{
  enabled && (
    <script is:inline>
      {`(function(c,l,a,r,i,t,y){c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);})(window, document, "clarity", "script", "xo2ruc05sy");`}
    </script>
  )
}
